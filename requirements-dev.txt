# DevSecOps CI Python tooling dependencies.
# Kept intentionally small for fast and reproducible lint jobs.
# Pin ansible-core explicitly to avoid upstream breaking API changes.
ansible-core>=2.17,<2.18
ansible-lint>=24.12,<25
